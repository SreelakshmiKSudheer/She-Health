# ---------------- Core API ----------------
fastapi==0.110.0
uvicorn[standard]==0.29.0

# ---------------- Database ----------------
sqlalchemy==2.0.29
pydantic==2.12.5
pydantic-settings==2.13.1

# SQLite (comes with Python, so no need to install separately)

# If using MongoDB
motor==3.7.1
pymongo==4.16.0

# ---------------- ML & Data ----------------
# numpy 1.26.4 does not have Windows wheel for Python 3.13 in some environments.
# Use the compatible wheel-based version to avoid building from source.
numpy==2.4.4
pandas==2.2.3
scikit-learn==1.7.2
xgboost==2.0.3
joblib==1.3.2

# ---------------- Utilities ----------------
python-multipart==0.0.9
python-dotenv==1.0.1
